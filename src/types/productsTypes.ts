export type TProduct = {
  _id: string;
  name: string;
  category: string;
  description: string;
  images: string[];
  price: number;
  stock: number;
  isDeleted: boolean;
  createdAt: string;
  updatedAt: string;
};
