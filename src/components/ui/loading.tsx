const Loading = () => {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <div className="animate-spin rounded-full size-20 border-t-4 border-b-4 border-primaryColor"></div>
    </div>
  );
};

export default Loading;
